export interface User { // Define a interface User para padronizar a estrutura dos dados.
    cliente_id: number; 
    nome: string; 
    endereco: string; 
    cep: string; 
    data_de_nascimento: string; 
    telefone: string; 
} 